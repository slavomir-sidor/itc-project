profile.${DOM}.  IN      A       ${IP}
email.${DOM}.  IN      A       ${IP}
jenkins.${DOM}.  IN      A       ${IP}
git.${DOM}.  IN      A       ${IP}
lamp.${DOM}.  IN      A       ${IP}
tomcat.${DOM}.  IN      A       ${IP}
wamp.${DOM}.  IN      A       127.0.0.1
*.wamp.${DOM}.  IN      A       127.0.0.1